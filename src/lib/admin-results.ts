// Shared validation bounds for admin result entry (separate from user
// prediction caps — real finals can in theory exceed 20, so we allow more).

export const ADMIN_SCORE_MIN = 0;
export const ADMIN_SCORE_MAX = 99;
